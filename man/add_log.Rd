% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_log.R
\name{add_log}
\alias{add_log}
\title{Log Messages to Console and Optional File}
\usage{
add_log(message, level = "INFO", log_file = NULL)
}
\arguments{
\item{message}{A character string representing the message to be logged.}

\item{level}{A character string indicating the log level. One of "\code{FATAL}",
\code{"ERROR"}, \code{"WARN"}, \code{"SUCCESS"}, \code{"INFO"}, \code{"DEBUG"}, or "\code{TRACE}".
Defaults to \code{"INFO"}.}

\item{log_file}{A character string specifying the path to the log file.
Defaults to \code{NULL} (messages not written to a file).}
}
\value{
This function does not return a value; it is used for its side effect of
logging messages.
}
\description{
A utility function for logging messages to the console and optionally
to a specified log file using the \code{logger} package.
This function supports multiple log levels, including \code{INFO}, \code{DEBUG},
\code{WARN}, and \code{ERROR}.
}
\details{
\itemize{
\item This function uses the \code{logger} package to manage logging.
\item By default, all log messages are printed to the console.
\item If \code{log_file} is specified, log messages are also written to the
specified file.
\item The \code{log_formatter} uses the \code{formatter_glue} format for interpolating
variables in the log message.
}
}
\examples{
# Log an informational message
add_log(message = "Application started.")

# Log a debug message to both console and file
add_log(message = "Debugging variable x.", level = "DEBUG", log_file = "debug_log.txt")

# Log a warning message without saving to a file
add_log(message = "Potential issue detected.", level = "WARN", log_file = NULL)

# Log an error message to a specific file
add_log(message = "An error occurred.", level = "ERROR", log_file = "errors_log.txt")

}
\seealso{
\itemize{
\item \code{\link[logger:log_appender]{logger::log_appender()}} for customizing log destinations.
\item \code{\link[logger:log_formatter]{logger::log_formatter()}} for customizing log message formats.
\item \code{\link[logger:log_level]{logger::log_info()}}, \code{\link[logger:log_level]{logger::log_debug()}}, \code{\link[logger:log_level]{logger::log_warn()}}, \code{\link[logger:log_level]{logger::log_error()}} for log level-specific functions.
}

Other \strong{Utility Functions}: 
\code{\link{log_message}()},
\code{\link{scatter_plot}()}
}
\concept{\strong{Utility Functions}}
